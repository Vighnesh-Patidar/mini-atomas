#pragma once

// System interface + SystemScheduler — see ARCHITECTURE.md §5
// Pre-v0.1: hazard model for non-component resources (NeighbourTable,
// transport buffers) is unresolved.

namespace mith {

} // namespace mith
