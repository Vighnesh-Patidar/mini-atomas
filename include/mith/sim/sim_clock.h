#pragma once

// SimClock — deterministic virtual clock, see ARCHITECTURE.md §9.2
// Pre-v0.1: determinism scope (single-threaded vs replay) is unresolved.

namespace mith::sim {

} // namespace mith::sim
